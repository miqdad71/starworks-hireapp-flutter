import 'package:flutter/material.dart';
import '../resetpassword/components/body.dart';
class ResetPassword extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
