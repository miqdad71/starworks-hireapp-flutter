import 'package:flutter/material.dart';
import '../onboard/components/body.dart';
class OnBoard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}